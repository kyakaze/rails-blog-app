<%= form_for(comment, :url => blog_comment_path(blog, comment)) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :body %><br />
    <%= f.text_area :body, class:"form-control", placeholder: "Your feedback means a lot to us!" %>
  </div>
  <div class="actions">
    <%= f.submit class:"btn btn-dark"%>
  </div>
  </br>

<% end %>

